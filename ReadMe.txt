Project Information:
- A fictitious raw dataset (dirty and messy) in JSON format, containing 65 records is uploaded to cloud object data store,
  data lake from a local windows machine
- Using data factory copy-data pipeline, the data is migrated to cosmos DB transactional store for onward near 
  real-time auto-sync to cosmos DB analytical store for future data cleaning, transformation and analytics
